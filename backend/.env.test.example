# E2E Testing Environment Variables
# Copy this file to .env.test and fill in your test credentials

# Backend server URL (usually local during development)
BACKEND_URL=http://localhost:3001

# Supabase credentials (same as main .env)
SUPABASE_URL=your_supabase_url
SUPABASE_ANON_KEY=your_anon_key

# Test user credentials (create this user in Supabase Auth)
TEST_EMAIL=test@jengu.com
TEST_PASSWORD=test123456

# Notes:
# - Create the test user in Supabase dashboard → Authentication → Users
# - Make sure backend server is running before running E2E tests
# - Run tests with: pnpm run test:e2e
